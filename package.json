{
  "name": "healthmap-fullstack",
  "version": "1.0.0",
  "description": "HealthMap MERN Stack Application",
  "main": "backend/server.js",
  "scripts": {
    "build-prod": "(cd backend && npm install) && (cd frontend && npm install && npm run build)",
    "start": "cd backend && npm start",
    "dev": "concurrently \"cd backend && npm run dev\" \"cd frontend && npm run dev\""
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
